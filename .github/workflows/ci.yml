name: CI Placeholder

on:
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Confirm setup
        run: echo "CI placeholder succeeded"
