# Web Export

1. Open the project in Godot 4.5.1 or newer.
2. Configure the HTML5 export preset to target this folder.
3. Use **Project → Export → HTML5** to generate the build.
4. Upload the exported contents of this directory to CrazyGames or your hosting platform.

> This folder only tracks instructions; generated builds should not be committed.
